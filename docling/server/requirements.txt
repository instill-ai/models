# Docling MLX host server — validated stack (Apple Silicon / Metal).
#
# Unlimited-OCR MXFP8 is packaged as a DeepSeekOCR mlx-vlm model with MXFP8 quantization,
# which needs the current 0.6.x loader/quantization support.
mlx-vlm==0.6.3
transformers>=5.5,<6
docling-core
huggingface_hub
pillow
PyMuPDF          # PDF -> page images (fitz)
fastapi
uvicorn[standard]

# Ray Serve front (serve_app.py) — multi-replica, page-parallel. Host-native arm64 (Metal).
ray[serve]
